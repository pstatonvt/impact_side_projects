def checkOnlineResourceDescription(self, val):
    self.storage = []
    self.repeat = []
    storage.append(val)
    for i in storage:
        if i == i:
            repeat.append(i)
    print("Description are being repeated:", repeat)
